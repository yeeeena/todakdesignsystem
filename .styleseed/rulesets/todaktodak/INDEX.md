# 토닥토닥 디자인 가이드 — 허브 (MOC)

> 이 문서는 **옵시디언 링크 허브**입니다. 디자인 기준 자체는 [[DESIGN]]부터 읽으세요.
> 여기서는 파일 간 연결과 옵시디언 연동만 다룹니다.
>
> ⚠️ 자료는 3단입니다 — 🟢 현재 기준(DESIGN·RULESET·COMPONENTS·tokens·checks·reference-board) /
> 🟡 참고(evidence·adapter·이 문서) / ⚪ 히스토리(패키지 내 프로토타입 스냅샷·참고자료). 상세는 [[DESIGN]] §0.

## 파일 구성과 용도

| 파일 | 무엇 | 언제 보나 |
|---|---|---|
| [[DESIGN]] | **가장 먼저 읽는 문서** — 현재 디자인 기준의 진입점 | **디자인 작업을 시작할 때** |
| [[RULESET]] | 12축 디자인 계약 (판단 기준) | **새 화면을 설계할 때** — "이 앱답게"의 정의 |
| [[COMPONENTS]] | 컴포넌트·자산 인벤토리 (구현 사전) | **화면을 만들거나 고칠 때** — 있는 부품 먼저 찾기 |
| [[checks]] | 필수/금지 tell + 회귀 시나리오 | **작업 끝나고 셀프 리뷰할 때**, PR 전 |
| `tokens.json` | 기계가독 토큰 값 | 정확한 수치가 필요할 때 (색·라운딩·타이밍) |
| `evidence.json` | 출처·신뢰도·**미해결 항목** | 팀 결정이 필요한 것 확인할 때 |
| `adapter.json` | 캔버스 계약·번들 레시피 | 공유용 단일 HTML 빌드할 때 |
| `reference-board.html` | 이미지 내장 시각 보드 | 눈으로 확인할 때 (브라우저로 열기) |
| [[TOKEN-MAPPING.draft]] | 토큰 정리 매핑 **초안 (검수 전)** | 토큰을 정리할 때 — 🔶 판단 4건 확정 후 적용 |

## 추천 워크플로우

- **처음이거나 오랜만이면**: [[DESIGN]] §0에서 무엇이 현재 기준인지 먼저 확인
- **새 화면 만들기**: [[RULESET]] §해당 축 → [[COMPONENTS]]에서 부품 조합 → 완성 후 [[checks]] 통과
  (문구의 문체·용어는 **별도 라이팅 문서**가 정본. 화면에 물린 필수 고지만 [[checks]] §문구)
- **디자인 리뷰**: [[checks]]만 놓고 화면과 대조 (tell 있음? anti-tell 없음?)
- **값 질문** ("라운딩 몇이었지?"): `tokens.json` 검색
- **"이거 왜 이렇게 됐지?"**: `evidence.json`의 출처·미해결 확인
- **팀 공유**: `reference-board.html` 전달 또는 프로토타입 단일 HTML 재빌드(`adapter.json` 레시피)

## 연결 구조 (링크 그래프)

```
STYLESEED.md (프로젝트 락, 루트)
 └─ INDEX (여기) ─┬─ RULESET ──┬─ COMPONENTS (상세 스펙 위임)
                  │            ├─ checks (검증 위임)
                  │            └─ reference-board.html (시각 증거)
                  ├─ COMPONENTS ── prototype/todaktodak-working.html (소스)
                  ├─ checks ── §문구(화면에 물린 필수 고지만)
                  └─ evidence.json

  ⚪ 참고자료/ (과거 배경 — 현재 기준 아님)

           (라이팅 문서 — 별도 관리, 여기서 링크만) ─┘
```

옵시디언 그래프에서 허브가 되도록 이 문서에서 전부 링크: [[DESIGN]] · [[RULESET]] · [[COMPONENTS]] · [[checks]] · [[FOLLOWUP]]
관련 외부 노트: 프로젝트 락 `STYLESEED.md`, 과거 콘텐츠 설계 `신별_동적화_설계.md` — 공유 패키지에서는 둘 다 `참고자료/`에 있습니다.
**둘 다 히스토리 자료입니다.** 신별 콘텐츠·상담 내용·매칭의 현재 기준은 구현 기준 문서입니다([[DESIGN]] §0).

**라이팅 문서는 이 폴더에서 관리하지 않습니다.** 목소리·어조·용어표는 별도 문서가 정본이니,
볼트에 들어오면 이 줄에 링크를 걸어주세요 → `[[여기에 라이팅 문서명]]`
디자인 가이드에는 **화면에 물린 필수 고지**(면책·개인정보·공유 범위)만 [[checks]] §문구에 남겨뒀습니다.

## 옵시디언 연동 시 주의 (중요)

1. **닷폴더 문제**: 옵시디언은 `.styleseed/` 같은 숨김 폴더를 인덱싱하지 않습니다.
   - 저장소를 직접 여는 경우 → 보이는 경로로 심링크가 걸려 있으니 **`docs/design-guide/`** 로 접근하세요.
     볼트에 정상 표시되고 원본과 자동 동기화됩니다(복사본 아님).
   - **공유 패키지(zip)로 받은 경우** → 압축을 풀어 폴더째 볼트에 넣으면 됩니다.
     이때는 스냅샷이라 자동 동기화되지 않습니다 — 갱신본은 다시 받아 덮어쓰세요.
2. **링크 방식**: 문서 간 링크는 표준 마크다운 상대경로라 옵시디언에서 그대로 동작합니다.
   위키링크(`[[문서명]]`)로 걸려면 볼트 설정 → "Files & Links → New link format: relative" 권장.
3. **HTML/JSON**: `reference-board.html`은 옵시디언이 렌더하지 못하니 "기본 앱으로 열기"로.
   json은 코드블록 뷰로만 보입니다 — 수치 검색용으로 쓰세요.
4. **편집 규칙**: 이 폴더 문서를 고치면 곧 디자인 정본이 바뀌는 것입니다.
   구현과 어긋나게 수정하지 말고, 바뀐 구현 → 문서 갱신 순서로. (자동 검증: COMPONENTS는
   CSS 클래스 152종 전수 커버 기준 — 클래스를 추가했다면 문서에도 추가)

## 현재 미해결

확인 필요 항목은 [[DESIGN]] §7에 정리돼 있고, 기존 패키지에서 손봐야 할 것은 [[FOLLOWUP]]에 있습니다.
아래는 `evidence.json`과 동기화된 요약입니다.

- 작은 실천 제목·부적 리드 5신분 — 초안, 팀 확정 대상
- 05·06 '다음 단계' 라벨 — "~하기" 규칙 예외, 라이팅 문서 쪽 결정 대기 (04 하단 안내는 경로별 분기로 해결)
- 신별 라벨톤(08·10 강조 텍스트)이 룰셋 §6과 충돌 — 별도 티어로 기재 vs charcoal 통일 결정 필요
- 체크리스트 ✓ 문자 유지 vs SVG 통일
- 피그마 신규 로더(신별 파스텔 배경 로딩 컷, 트랙 #cbc9c3) — 구현 미반영, 적용 여부 결정 필요
- STYLESEED.md 락의 terracotta primary(#9B5132) ↔ 실제 charcoal CTA 불일치
