# 토닥토닥 디자인 기준 (DESIGN.md)

> **디자인 작업을 시작하기 전에 이 문서를 먼저 읽습니다.**
> 무엇을 기준으로 삼아야 하는지, 어떤 자료가 현재 기준이고 어떤 자료가 참고·히스토리인지 여기서 정리합니다.
>
> 최종 갱신 2026-08-28 · 기준 구현 `prototype/todaktodak-working.html` (화면 16종)

---

## 0. 먼저 — 무엇을 기준으로 삼는가

자료마다 "정본"의 범위가 다릅니다. **문구 관리 문서의 「구현 정본」이 이 배분의 근거**이고,
아래는 그것을 디자인 작업 관점으로 옮긴 표입니다.

| 판단 대상 | 정본 | 이 문서에서의 취급 |
|---|---|---|
| 시각 토큰 · 레이아웃 · 컴포넌트 · 모션 · 접근성 | **디자인 가이드 = 이 패키지** | §2–§5에 현재 기준으로 확정 서술 |
| 01–16 화면 구성 · CTA · 시연 인터랙션 | **`토닥토닥_프로토타입_단일파일.html`** | §4에 확정 서술 (해시 일치 확인 완료) |
| 상태 · 이벤트 · 예외 · Acceptance Criteria | 구현 기준·기능명세 00–14 | 디자인 문서에서 정의하지 않음 |
| 신별 담당 영역 · 매칭 · 동적 결과 | 구현 기준 문서 A/B/02 | 정의하지 않음. 패키지 `참고자료/`는 과거 배경 |
| 화면 고정 문구 · 개발 키 · 언어별 버전 | **Copy Master** (165건 / 16화면) | 문구를 **정의하지 않음**. `copy_id`로만 참조 |
| 부적 조립 · 이미지 원본 | 구현 기준 C/13 · `부적_통합문서.html` | 정의하지 않음 |

### 화면 정본 확인 (2026-08-28)

문구 관리 문서에 등록된 **화면 기준 SHA-256**과 이 패키지의 프로토타입이 **일치합니다.**

```
등록 해시  bbcd1ee786c145fa3625f88e428fe1af68b0b3cbb08c23dc163bd1a40c5489ed
패키지 파일 bbcd1ee786c145fa3625f88e428fe1af68b0b3cbb08c23dc163bd1a40c5489ed  ✅
```

즉 **패키지 안의 프로토타입은 과거 스냅샷이 아니라 화면 구성의 정본 그 자체**입니다.
Copy Master도 문구 165건 중 100건의 `source`를 이 파일(별칭 `final_design_html`)로 기록하고 있습니다.
→ §4의 화면 서술을 그대로 기준으로 삼아도 됩니다.

### 자료의 3단 구분

| 구분 | 자료 | 의미 |
|---|---|---|
| 🟢 **현재 기준** | `DESIGN.md` · `RULESET.md` · `COMPONENTS.md` · `tokens.json` · `checks.md` · `reference-board.html` · `토닥토닥_프로토타입_단일파일.html` | 지금 디자인을 판단·구현하는 근거 |
| 🟡 **참고** | `evidence.json` · `adapter.json` · `INDEX.md` · `FOLLOWUP.md` | 결정 배경·빌드 정보. 디자인 판단의 근거는 아님 |
| ⚪ **히스토리 (기준 아님)** | `참고자료/` 전체 | 과거 디자인 결정·콘텐츠 설계 배경 |

> ⚠️ `참고자료/신별_동적화_설계.md`는 **신별 콘텐츠의 정본이 아닙니다.**
> 신별 담당 영역·매칭·동적 결과의 현재 기준은 구현 기준 문서 A/B/02입니다.

## 1. 디자인 방향과 핵심 원칙

토착신이 사용자의 고민을 읽고 부적으로 남겨주는 **감정 케어 서비스**입니다.
점술이 아니라 **오늘의 고민을 정리하는 도구**라는 위치가 디자인 전반을 지배합니다.

### 다섯 가지 원칙

**1) 선형 흐름 — 한 화면 = 한 결정**
대시보드·탐색형이 아닙니다. 화면마다 주 행동 1개 + 보조 행동 2개 이하.
진입은 두 갈래(**고민부터** / **신부터**)지만 고민 입력 단계에서 한 흐름으로 합류합니다.

**2) 일러스트가 절반**
몰입 화면은 풀블리드 일러스트가 첫 시선이고, 텍스트·버튼은 그 위에 코드 레이어로 얹힙니다.
캐릭터가 카드 위로 살짝 겹치는 배치, 실루엣대로 반응하는 탭 — 이런 것이 이 서비스의 인상을 만듭니다.

**3) 위계는 색이 아니라 불투명도와 굵기로**
warm mono가 기본입니다. 크림 바탕 + 차콜 잉크. 유채색으로 위계를 만들지 않습니다.
색은 **신별 몰입 순간**에만 크게 씁니다.

**4) 재촉하지 않는 톤**
"~해도 괜찮아요", "안 해도 돼요"가 반복되는 조력자의 목소리입니다.
느낌표·이모지를 쓰지 않고, 감정은 어휘로 전달합니다.
※ 실제 문구는 **문구 관리 문서가 정본**입니다. 이 원칙은 디자인이 그 톤을 방해하지 않기 위한 것입니다.

**5) 정리 도구임을 반복해서 밝힌다**
면책·개인정보·공유 범위 고지가 특정 화면에 붙어 있습니다.
**디자인을 고칠 때 이 고지 자리가 사라지지 않아야 합니다**(§6, `checks.md` §문구).

---

## 2. 토큰 사용 원칙

정확한 값은 `tokens.json`, 설명이 붙은 표는 `COMPONENTS.md` §1에 있습니다. 여기서는 **쓰는 규칙**만.

### 2-1. 컬러

| 층 | 값 | 용도 |
|---|---|---|
| 배경 | `--cream #f7f4ed` | 페이지 바탕 |
| 표면 | `--surface #FFFDFC` | 카드·버튼 표면 |
| 잉크 | `--charcoal #1c1c1c` | 본문·제목·주 버튼 |
| 잉크 사다리 | `ink-83 / 82 / 40 / 08 / 04 / 03` | 보조 텍스트 → 테두리 → 헤어라인 → 틴트 |

- **순검정 `#000` 금지.** 잉크는 항상 차콜입니다.
- **위계는 잉크 불투명도와 굵기로.** 색을 바꿔 강조하지 않습니다.
- **`<button>`에는 `color`를 반드시 지정합니다.** 브라우저 기본값이 순검정이라 그대로 두면 원칙이 깨집니다.
- 새 코드는 **SEED 역할 토큰(`--seed-color-*`)** 을 씁니다. 레거시 별칭(`--blue-500` 등)은 참조용입니다.

**신별 색은 두 층입니다 — 섞어 쓰지 마세요.**

| 층 | 어디에 | 규칙 |
|---|---|---|
| **딥컬러** (`bg`) | 상담 결과 배경, 저장/공유 딤 | **배경 전용.** 버튼·테두리 등 컨트롤 색으로 쓰지 않음 |
| **라벨톤** (`label`) | 결과·부적 화면의 미니타이틀, 신 컨텍스트의 이름 | 표면 위 대비 4.5:1을 통과하도록 어둡게 조정한 값. **목록·비교 UI에서는 쓰지 않음**(신들이 동등하게 읽혀야 함) |

> ⚠️ 이 두 층의 경계는 아직 완전히 정리되지 않았습니다 → §7 확인 필요 1번.

**시맨틱 색**은 안전 안내·생성 실패 화면 전용입니다. 상태를 무지개로 표현하지 않습니다.

### 2-2. 타이포그래피

**Pretendard 단일 패밀리.** 페이지 제목 24 → 인용 20 → 카드 제목 17 → 본문 15 → 보조 14 → 미니라벨 13 → 캡션 12.

- **자간은 항상 0 이하.** 양수 자간·uppercase 오버라인을 쓰지 않습니다.
- **Regular(400) 이하를 쓰지 않습니다.** 최소 Medium(500).
- 한글 줄바꿈이 어색해지는 자리(다크 배경 제목, 좁은 폭의 이름)는 `word-break: keep-all`.
- 이름 뒤 조사(이/가)는 받침에 따라 자동 선택합니다 — 하드코딩하지 않습니다.

### 2-3. 간격 · 라운딩 · 레이아웃

- **캔버스 390×844**, 본문 좌우 여백 24px(콘텐츠 폭 342px), **8px 그리드**.
- 카드 패딩 18–20 · 카드 간 12 · 섹션 간 24–26. 허용 오차 ±4px.
- 라운딩 사다리: `8 / 12 / 14 / 16 / 20 / 24 / 32 / 999`. **카드는 16**, 시트 상단은 20–24.
- 그림자는 뉴트럴 저강도만. 하드섀도는 **시작 화면 버튼 하나의 예외**이고 다른 곳으로 번지지 않습니다.

### 2-4. 모션

- duration `120 / 200 / 320ms`, ease 2종. **UI 전환은 320ms 이하**(부적 뒤집기 640ms만 예외).
- 눌림은 오버레이로: 어두운 버튼엔 밝은 오버레이, 밝은 버튼엔 어두운 오버레이.
- **모든 모션은 `prefers-reduced-motion`에서 꺼집니다.** 새 모션을 넣으면 이 대응을 같이 넣습니다.
- 무한 회전 스피너·스켈레톤 시머를 쓰지 않습니다(§3-6).

---

## 3. 핵심 컴포넌트 사용 기준

전체 목록과 정확한 수치는 `COMPONENTS.md`, 눈으로 볼 때는 `reference-board.html`.

### 3-1. 버튼

| 위계 | 형태 | 언제 |
|---|---|---|
| 주 | 차콜 채움 · 52px · r12 | 화면당 1개 |
| 보조 | 표면 + 테두리 · 52px | 되돌아가기, 대안 행동 |
| 3차 | 고스트 / 밑줄 텍스트 | 부가 행동 |

- 화면 하단에 **버튼이 3개** 필요하면: 주 행동 1개를 전폭으로 위에, 보조 2개를 한 줄로 아래.
  같은 위계 3개를 한 줄에 늘어놓지 않습니다.
- 페어 버튼은 16px 라벨, 단독 CTA는 18px.
- 버튼 라벨은 **행동을 말하는 형태**로. 실제 문구는 문구 관리 문서를 따릅니다.

### 3-2. 카드

기본(표면 + 얇은 테두리) / 소프트(연한 배경, 테두리 없음) / 틴트 3단.
**딥컬러 배경 위에서는 2px 차콜 테두리**로 톤을 눌러줍니다.
카드 패딩이 14px 미만으로 촘촘해지지 않게 합니다.

### 3-3. 입력 · 칩

- 텍스트영역은 포커스 시 테두리가 2px 차콜로 굵어집니다.
- 칩은 36px 필. 선택되면 **차콜 채움 + 굵기 상승** — 색이 아니라 채움으로 표시합니다.
- 점수 선택(1–5)은 연한 틴트 + 차콜 테두리.

### 3-4. 바텀시트

세 종류가 같은 문법을 공유합니다.

| 시트 | 성격 |
|---|---|
| 신 소개 시트 | 화면 위로 올라오는 전역 시트. 상단 라운딩 24, 최대 높이 76% |
| 스텝 시트 (고민 정리) | 화면 하단 고정 패널. **상단 위치가 높이가 아니라 폭에 비례** — 일러스트와의 겹침을 기기 폭에 관계없이 유지 |
| 신 선택 시트 | 전역 시트를 재사용하고 내용만 목록으로 교체 |

**목록형 시트에서 항목은 카드 문법 그대로**(표면 + 얇은 테두리 + r16) 씁니다.
선택 표시는 칩·점수와 같은 관용구(**차콜 테두리 + 연한 틴트**)이고, 신별 색을 쓰지 않습니다.

### 3-5. 아이콘

뒤로·닫기·셰브런은 **전부 24px SVG, stroke 1.8**.
문자 글리프(`✕` `›`)나 이모지를 아이콘으로 쓰지 않습니다 — 굵기와 정렬이 폰트에 끌려갑니다.

### 3-6. 로더 · 진행 표시

- **로딩 = 캐릭터 링 로더.** 원형 링이 채워지는 동안 신 아바타가 순환합니다.
- **진행 = 상단 HUD 바.** 일러스트 위에 흰 라벨 + 가는 진행바.
- 3-dot 로더는 레거시(미사용), 스켈레톤 시머는 금지입니다.

---

## 4. 화면에서 반드시 유지해야 하는 특징

> 아래는 **화면 정본(`토닥토닥_프로토타입_단일파일.html`) 기준**입니다(§0 해시 확인 완료).
> "이 특징이 사라지면 서비스가 서비스답지 않게 된다"는 **디자인 자산**을 적었습니다.
> 화면 상태·분기·예외 동작은 구현 기준·기능명세가 정본이라 여기서 다루지 않습니다.

| 화면 | 유지해야 하는 것 |
|---|---|
| 신 목록 | 캐릭터를 누르면 **실루엣 모양 그대로** 살짝 커졌다가 소개 시트가 뜸. 뒤 캐릭터가 비치지 않아야 함 |
| 고민 정리 (3단계) | 상단 일러스트 + 하단 라운드 시트의 분할. 시트 상단은 **폭 비례** |
| 상담 결과 | 신별 딥컬러 몰입 배경 + **캐릭터가 카드 위로 살짝 겹치는 배치** |
| 나만의 부적 | 부적 카드를 탭하면 앞뒤가 뒤집힘. 진입 시 1회 힌트 |
| 저장/공유 | 부적 아트를 크게 흐린 배경 + 신별 딤. 카드 모서리는 라운딩으로 잘라냄 |
| 로딩 (2종) | 캐릭터 링 로더 |
| 안내 3종 | 제목만 시맨틱 색, 본문은 기본 잉크 |

**진입 경로에 따라 갈리는 것** — 신을 골라 들어왔는지가 화면에 남아 세 가지를 바꿉니다.

1. 고민 정리 3단계의 **상단 일러스트** (신을 골라 들어왔을 때만 신별 아트, 아니면 공통)
2. **뒤로 가기 목적지** (신 목록 / 시작 화면)
3. 고민 입력의 **신 컨텍스트 표시 시점** (이미 골랐으면 바로, 아니면 고민을 적은 뒤 추천)

> 고민 입력 화면에서 신을 바꿔도 상단 일러스트는 공통을 유지합니다.
> 배경이 통째로 갈리면 화면이 바뀐 것처럼 읽히기 때문입니다.

**필수 고지가 붙는 자리** — 문구는 Copy Master가 정본이고, 디자인은 **자리**를 지킵니다.

Copy Master에서 `safety_locked=Yes`로 잠긴 문구가 **19건**, 아래 화면에 걸쳐 있습니다.
안전 검토 없이 수정할 수 없는 문구이므로, **표시 공간을 없애거나 스크롤 밖으로 밀지 않습니다.**

| 화면 | 자리 | 무엇을 밝히는가 |
|---|---|---|
| 01 시작 | 하단 캡션 | 점술이 아니라 정리 도구라는 면책 |
| 04 고민 입력 | 입력 아래 helper | 개인정보를 적지 않아도 된다는 안내 |
| 10 나만의 부적 | 하단 캡션 | 부적이 결과를 약속하지 않는다는 면책 |
| 11 저장/공유 | 설명 · 저장 완료 toast | 공유물에 고민 원문이 포함되지 않는다는 안내 |
| 14 안전 안내 | 화면 전체 (7건) | 판단·진단하지 않고 전문 기관으로 연결 |
| 15 내부 요청 안내 | 화면 전체 (7건) | 공개 가능 범위 안내 |

실제 텍스트는 `copy_id`로 조회합니다(`checks.md` §문구에 매핑표).

## 5. 접근성 · 반응형 · 텍스트 길이

### 접근성

- **대비 4.5:1 이상.** 특히 딥컬러 배경 위 텍스트는 표면색을 쓰고, 반투명 흰색을 본문에 쓰지 않습니다.
- 신별 라벨톤 6색은 표면 위 4.5:1을 통과하도록 조정된 값입니다 — 임의로 밝게 바꾸지 마세요.
- 아이콘 버튼에는 레이블을 답니다.
- 탭 가능한 요소는 실제 버튼 요소로 만듭니다(카드 전체가 눌리는 경우 포함).
- 모션은 `prefers-reduced-motion` 대응이 필수입니다.
- ⚠️ **키보드 포커스 링이 아직 정의돼 있지 않습니다** → §7 확인 필요 4번.

### 반응형

- **390px 단일 목표.** 520px 이하에서 프레임만 축소되고, 태블릿·데스크톱은 정의돼 있지 않습니다.
- 배경 좌표는 비율 기반이라 폭이 바뀌어도 따라옵니다.
- 스텝 시트 상단이 **폭에 비례**하는 이유가 이것입니다 — 높이 기준으로 바꾸면 겹침이 어긋납니다.

### 텍스트 길이 대응

- 신 이름 길이가 다릅니다(2–4자). **이름이 들어가는 자리는 가장 긴 이름 기준으로 확인**하세요.
- 조사는 받침에 따라 자동 선택됩니다.
- 좁은 폭의 텍스트는 `keep-all`로 어절 중간에서 끊기지 않게 합니다.
- ⚠️ 본문이 예상보다 길어질 때의 처리(말줄임 / 접기)가 정의돼 있지 않습니다 → §7 확인 필요 5번.

---

## 6. 각 파일을 언제 참고하나

**기본 역할** — 요청받은 배분을 그대로 옮긴 표입니다.

| 파일 | 기본 역할 | 구분 |
|---|---|---|
| `RULESET.md` | 새 화면이나 디자인을 판단할 때 사용하는 디자인 원칙 | 🟢 |
| `COMPONENTS.md` | 화면 제작·수정 시 사용하는 컴포넌트와 수치 기준 | 🟢 |
| `tokens.json` | 색·간격·라운딩·모션 등 구현 수치 | 🟢 |
| `checks.md` | 디자인 작업 완료 후 회귀·필수 요소·금지 요소 점검 | 🟢 |
| `reference-board.html` | 토큰·컴포넌트·모션·에셋을 눈으로 비교하는 참고 보드 | 🟢 |
| `evidence.json` | 디자인 결정의 근거와 당시 미결 사항 참고 | 🟡 |
| `adapter.json` | 기존 프로토타입/공유용 HTML 제작 관련 참고 정보 | 🟡 |
| 패키지 내부 `토닥토닥_프로토타입_단일파일.html` | 디자인 가이드 작성 당시 화면 스냅샷 — **확인 결과 현재 화면 정본과 동일**(아래 참고) | 🟢 |
| `참고자료/` | 과거 디자인 결정·콘텐츠 설계 배경 확인용. 현재 제품 내용의 기준으로 사용하지 않음 | ⚪ |

> 📌 **프로토타입 행만 원래 배분과 달라졌습니다.**
> 당초에는 "작성 당시 스냅샷이므로 현재 최종 프로토타입과 구분"이었으나,
> 문구 관리 문서에 등록된 화면 기준 SHA-256과 **바이트 단위로 일치**하는 것이 확인됐습니다(§0).
> 즉 구분해야 할 별개의 파일이 아니라 **같은 파일**입니다. 화면 구성·CTA·인터랙션의 근거로 그대로 쓰시면 됩니다.

**이번에 추가된 문서** — 위 목록에 없던 파일입니다.

| 파일 | 기본 역할 | 구분 |
|---|---|---|
| `DESIGN.md` (이 문서) | **가장 먼저 읽는 문서.** 현재 디자인 기준의 진입점 | 🟢 |
| `FOLLOWUP.md` | 기존 파일 중 최신 기준과 맞지 않아 수정이 필요한 목록 | 🟡 |
| `INDEX.md` | 옵시디언에서 문서를 연결·탐색할 때 쓰는 링크 허브 | 🟡 |
| `README.md` | 패키지를 처음 열 때 보는 구성·설치 안내 | 🟡 |

**작업 순서로 보면**

1. 시작 전 → `DESIGN.md` §0에서 무엇이 현재 기준인지 확인
2. 설계할 때 → `RULESET.md`
3. 만들 때 → `COMPONENTS.md` + `tokens.json` (눈으로 볼 땐 `reference-board.html`)
4. 끝내고 → `checks.md`
5. 막히면 → `evidence.json`(왜 이렇게 됐는지) · `FOLLOWUP.md`(아직 안 고쳐진 것)

---

## 7. 확인 필요 항목

디자인 문서에서 임의로 결정하지 않고 남겨둔 것들입니다.

### 실제 확인된 불일치 (조치 필요)

**1. 09 부적 생성 중 — 구현 문구가 Copy Master보다 오래됨** ⚠️

Copy Master V1.2(2026-08-14)에서 "부적 로딩 2건을 고정 에셋 조립 의미로 교정"했는데,
화면 정본 프로토타입은 아직 이전 문구입니다. Copy Master 165건 중 **불일치는 이 2건뿐**입니다.

| copy_id | 현재 화면 | Copy Master (정본) |
|---|---|---|
| `talismanLoading.hero.body.description` | …작은 실천을 모아 **나만의 부적 프롬프트를 만들고 있어요** | …작은 실천을 모아 **고정된 부적 요소를 조합하고 있어요** |
| `talismanLoading.check.image` | 부적 이미지 **생성 준비** | 부적 **조합 마무리** |

**디자인 관점에서 중요한 이유** — "생성"과 "조합"은 로더가 무엇을 하는 중인지에 대한
사용자의 이해를 바꿉니다. 부적이 즉석 생성물이 아니라 **고정 에셋 조립**이라는 것이
정본의 방향이므로, 로더 연출도 그 의미와 어긋나지 않아야 합니다.
→ 화면 정본 갱신 시 반영 필요. 파일이 바뀌면 등록 SHA도 함께 갱신해야 합니다.

**2. 화면 번호 체계** — Copy Master는 예외 화면을 **14·15·16**으로 번호를 매기는데,
디자인 가이드는 "예외 3종"으로 묶어 부릅니다. 문서 간 화면 지칭이 어긋나므로 정렬이 필요합니다.
(`COMPONENTS.md` 화면 맵 → FOLLOWUP)

### 아직 확인하지 못한 것

**구현 기준·기능명세 00–14 (Notion C)** 를 이 세션에서 열지 못했습니다.
화면 상태·분기·오류·로딩 동작은 디자인 문서에서 정의하지 않으므로 직접 충돌은 없지만,
아래는 대조해두면 좋습니다.

- 로딩 화면의 **실제 소요 시간**과 링 로더 애니메이션 길이(4.6초)가 맞는지
- 오류·지연 화면의 **분기 조건**이 현재 16번 화면 구성으로 커버되는지
- 신별 매칭 로직(A/B/02)이 04 신 추천 UI의 전제와 같은지

### 디자인 내부에서 아직 안 정해진 것

1. **신별 라벨톤의 지위** — 룰셋은 신별 색을 "배경 전용, 컨트롤 색 금지"로 정했는데,
   실제로는 결과·부적 화면의 미니타이틀에 라벨톤을 씁니다. 한 신은 두 값이 같아 규칙 위반처럼 보입니다.
   → 라벨톤을 별도 층으로 인정할지, 텍스트 강조를 차콜로 통일할지 결정 필요.
2. **체크 표시 아이콘** — 완료 표시만 문자 글리프(`✓`)입니다. SVG로 통일할지.
3. **로더 디자인** — 피그마에 신별 파스텔 배경의 새 로딩 컷이 있으나 구현 미반영. 적용 여부 결정 필요.
   (위 09 문구 교정과 함께 보면 좋습니다 — 로더가 전달하는 의미가 바뀌는 사안입니다.)
4. **키보드 포커스 링** — 정의돼 있지 않습니다. 접근성 기준으로 추가할지.
5. **긴 텍스트 처리** — 말줄임/접기 규칙이 없습니다. Copy Master에 `max_length`가 비어 있어
   디자인 쪽에서 상한을 제시할지 결정 필요.
6. **브랜드 락 불일치** — 과거 브랜드 문서의 주색(테라코타)과 현재 구현(차콜)이 다릅니다.
7. **다국어 레이아웃** — Copy Master에 EN·JA 칼럼이 있으나 현재 전부 비어 있습니다(`not_started`).
   번역이 들어오면 한국어보다 길어지는 문구가 생기므로, 그때 레이아웃 기준이 필요합니다.

## 8. 이 문서를 고칠 때

- 구현이 바뀌면 **구현 → 문서** 순서로 갱신합니다. 문서를 먼저 바꿔 구현과 어긋나게 두지 않습니다.
- 문구·제품 정책·신별 콘텐츠는 이 문서의 범위가 아닙니다. 해당 정본 문서로 넘깁니다.
- 판단이 갈리면 §0 표를 먼저 봅니다.
